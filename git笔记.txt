git init 
touch .gitignore //添加版本控制忽略列表
git add --verbose .
git commit -m '备注信息'
git remote add origin https://github.com/xxx/yyy.git //将本地库关联到远程库
git branch 分支  //创建本地分支
git pull origin 本地分支:远程分支 //将本地分支提交到远程分支

///清除版本控制,并且更新版本忽略列表
git rm -r --cached .

git add .

git commit -m 'update .gitignore'


///解决一个错误
git config --global core.autocrlf false

//可查看add列表
git add --verbose .

